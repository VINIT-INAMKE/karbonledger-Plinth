---
phase: 01-code-quality-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Carbonica/Validators/Common.hs
  - src/Carbonica/Utils.hs
  - src/Carbonica/Validators/ProjectVault.hs
  - src/Carbonica/Validators/UserVault.hs
  - src/Carbonica/Validators/CetPolicy.hs
  - src/Carbonica/Validators/Marketplace.hs
  - src/Carbonica/Validators/ProjectPolicy.hs
  - src/Carbonica/Validators/IdentificationNft.hs
  - src/Carbonica/Validators/CotPolicy.hs
  - smartcontracts.cabal
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-04

must_haves:
  truths:
    - "Common.hs is the single source of truth for all shared validation helpers"
    - "No duplicate helper functions exist anywhere in the codebase"
    - "Utils.hs is deleted and no module imports from it"
    - "All exported helper functions in Common.hs have INLINEABLE pragmas"
    - "The project compiles successfully with cabal build"
  artifacts:
    - path: "src/Carbonica/Validators/Common.hs"
      provides: "All shared helpers including migrated Utils.hs functions"
      exports: ["findInputByNft", "findOutputByNft", "findInputByOutRef", "extractDatum", "findConfigDatum", "findDatumInOutputs", "validateMultisig", "hasTokenInOutputs", "sumTokensByPolicy", "countTokensWithName", "hasSingleTokenWithName", "getMintedAmountForToken", "isInList", "countMatching", "tokenNameFromOref", "payoutExact", "payoutAtLeast", "payoutTokenExact", "mustBurnLessThan0", "getTokensForPolicy", "allNegative", "getTotalForPolicy", "sumQty", "isCategorySupported", "anySignerInList"]
    - path: "smartcontracts.cabal"
      provides: "Updated module list without Carbonica.Utils"
      contains: "Carbonica.Validators.Common"
  key_links:
    - from: "src/Carbonica/Validators/CetPolicy.hs"
      to: "Carbonica.Validators.Common"
      via: "import replacing local helpers"
      pattern: "import.*Carbonica\\.Validators\\.Common"
    - from: "src/Carbonica/Validators/Marketplace.hs"
      to: "Carbonica.Validators.Common"
      via: "import replacing Utils and local helpers"
      pattern: "import.*Carbonica\\.Validators\\.Common"
    - from: "src/Carbonica/Validators/ProjectVault.hs"
      to: "Carbonica.Validators.Common"
      via: "import replacing local helpers"
      pattern: "import.*Carbonica\\.Validators\\.Common"
    - from: "src/Carbonica/Validators/IdentificationNft.hs"
      to: "Carbonica.Validators.Common"
      via: "import replacing Utils"
      pattern: "import.*Carbonica\\.Validators\\.Common"
---

<objective>
Consolidate all shared helper functions into Common.hs as the single source of truth, eliminate Utils.hs, and remove all local duplicate helpers from validators.

Purpose: Every subsequent phase (vulnerability fixes, error codes, tests) depends on a clean, deduplicated helper layer. This is the foundation all other work builds on.
Output: Expanded Common.hs with all migrated functions, all validators importing from Common.hs, Utils.hs deleted, cabal updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-quality-foundation/01-CONTEXT.md
@.planning/phases/01-code-quality-foundation/01-RESEARCH.md

@src/Carbonica/Validators/Common.hs
@src/Carbonica/Utils.hs
@src/Carbonica/Validators/ProjectVault.hs
@src/Carbonica/Validators/UserVault.hs
@src/Carbonica/Validators/CetPolicy.hs
@src/Carbonica/Validators/Marketplace.hs
@src/Carbonica/Validators/ProjectPolicy.hs
@src/Carbonica/Validators/IdentificationNft.hs
@src/Carbonica/Validators/CotPolicy.hs
@smartcontracts.cabal

<interfaces>
<!-- Current Common.hs exports (executor extends this) -->
From src/Carbonica/Validators/Common.hs:
```haskell
module Carbonica.Validators.Common
  ( findInputByNft, findOutputByNft, findInputByOutRef
  , extractDatum, findConfigDatum, findDatumInOutputs
  , validateMultisig
  , hasTokenInOutputs, sumTokensByPolicy, countTokensWithName
  , hasSingleTokenWithName, getMintedAmountForToken
  , isInList, countMatching
  ) where
```

<!-- Current Utils.hs exports (all must migrate to Common.hs) -->
From src/Carbonica/Utils.hs:
```haskell
module Carbonica.Utils where
-- tokenNameFromOref :: TxOutRef -> TokenName
-- payoutExact :: PubKeyHash -> Integer -> [TxOut] -> Bool
-- payoutAtLeast :: PubKeyHash -> Integer -> [TxOut] -> Bool
-- payoutTokenExact :: PubKeyHash -> CurrencySymbol -> TokenName -> Integer -> [TxOut] -> Bool
-- mustBurnLessThan0 :: Value -> CurrencySymbol -> Bool
-- getTokensForPolicy :: Value -> CurrencySymbol -> [(TokenName, Integer)]
-- allNegative :: [(TokenName, Integer)] -> Bool
-- getTotalForPolicy :: Value -> CurrencySymbol -> Integer
-- sumQty :: [Integer] -> Integer
-- isCategorySupported :: BuiltinByteString -> [BuiltinByteString] -> Bool
-- countMatchingSigners (DUPLICATE of countMatching - DELETE)
-- verifyMultisig (DUPLICATE of validateMultisig - DELETE)
-- isInList (monomorphic DUPLICATE - DELETE)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Utils.hs functions into Common.hs and expand exports</name>
  <files>src/Carbonica/Validators/Common.hs, src/Carbonica/Utils.hs</files>
  <action>
Expand Common.hs with all unique functions from Utils.hs. Do NOT migrate duplicates (countMatchingSigners, verifyMultisig, isInList from Utils -- these already exist in Common.hs under the names countMatching, validateMultisig, isInList).

Add the following new imports to Common.hs (needed by migrated functions):
- `import qualified PlutusTx.Builtins as Builtins` (for tokenNameFromOref)
- `import PlutusLedgerApi.V3 (Address(..), Credential(..), Lovelace(..), BuiltinByteString, Value, TxOutRef)` (add to existing import)
- `import PlutusLedgerApi.V1.Value (flattenValue, lovelaceValueOf)` (add to existing import)

Add these new sections to Common.hs AFTER the existing "VALUE HELPERS" section:

1. **TOKEN NAME GENERATION** section:
   - `tokenNameFromOref :: TxOutRef -> TokenName` -- migrate verbatim from Utils.hs lines 41-43

2. **PAYOUT VERIFICATION** section:
   - `payoutExact :: PubKeyHash -> Integer -> [TxOut] -> Bool` -- migrate verbatim
   - `payoutAtLeast :: PubKeyHash -> Integer -> [TxOut] -> Bool` -- migrate verbatim
   - `payoutTokenExact :: PubKeyHash -> CurrencySymbol -> TokenName -> Integer -> [TxOut] -> Bool` -- migrate verbatim

3. **BURN VERIFICATION** section:
   - `mustBurnLessThan0 :: Value -> CurrencySymbol -> Bool` -- migrate verbatim
   - `getTokensForPolicy :: Value -> CurrencySymbol -> [(TokenName, Integer)]` -- migrate verbatim
   - `allNegative :: [(TokenName, Integer)] -> Bool` -- migrate verbatim
   - `getTotalForPolicy :: Value -> CurrencySymbol -> Integer` -- migrate verbatim
   - `sumQty :: [Integer] -> Integer` -- migrate verbatim

4. **CATEGORY VALIDATION** section:
   - `isCategorySupported :: BuiltinByteString -> [BuiltinByteString] -> Bool` -- migrate verbatim

5. Add to **LIST HELPERS** section:
   - `anySignerInList :: P.Eq a => [a] -> [a] -> Bool` -- NEW helper: `anySignerInList signers list = P.any (\s -> isInList s list) signers`. This replaces the local `anySignerInList` pattern in ProjectVault.

Every migrated function MUST have `{-# INLINEABLE functionName #-}` immediately above its definition. Use the `---- SECTION NAME ----` separator pattern for new sections.

Update the module export list to include all new functions.

Do NOT delete Utils.hs yet (that happens after imports are updated in Task 2).
  </action>
  <verify>
    <automated>cd E:/smartcontracts && cabal build 2>&1 | tail -20</automated>
  </verify>
  <done>Common.hs contains all unique Utils.hs functions plus anySignerInList, all with INLINEABLE pragmas. Module compiles. Export list updated.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all local duplicates and Utils imports across validators, delete Utils.hs, update cabal</name>
  <files>src/Carbonica/Validators/ProjectVault.hs, src/Carbonica/Validators/UserVault.hs, src/Carbonica/Validators/CetPolicy.hs, src/Carbonica/Validators/Marketplace.hs, src/Carbonica/Validators/ProjectPolicy.hs, src/Carbonica/Validators/IdentificationNft.hs, src/Carbonica/Validators/CotPolicy.hs, src/Carbonica/Utils.hs, smartcontracts.cabal</files>
  <action>
For each validator, consult the RESEARCH.md "Local Validator Duplicates - Must Remove" table and apply ALL replacements. Work through each file:

**ProjectVault.hs** (heaviest -- 7 local duplicates):
- Remove local `isInList` from where clause -> use `Common.isInList`
- Remove local `countMatchingSigners` -> use `Common.countMatching`
- Remove local `sumQty` -> use `Common.sumQty` (now migrated)
- Remove local `getTokensBurnedForPolicy` -> use `Common.getTokensForPolicy` (adapt: it returns `[(TokenName, Integer)]`, use with `Common.sumQty` if needed to get total)
- Remove local `verifyPaymentToAddress` -> use `Common.payoutTokenExact` (now migrated)
- Remove local `anySignerInList` -> use `Common.anySignerInList` (now migrated)
- Remove local `hasSigners` -> inline as `not (null signatories)` or `P.not (P.null signatories)` at call site
- Update import of `Carbonica.Validators.Common` to include all newly needed functions

**UserVault.hs:**
- Remove local `getTokensForPolicy` -> use `Common.getTokensForPolicy`
- Remove local `sumQty` or inline equivalent -> use `Common.sumQty`
- Remove local `findSelfInput` -> use `Common.findInputByOutRef`
- Add/update import of `Carbonica.Validators.Common` with needed functions

**CetPolicy.hs:**
- Remove local `getTotalMintedForPolicy` -> use `Common.sumTokensByPolicy` or `Common.getTotalForPolicy` (choose whichever matches the call site signature)
- Remove local `sumQty` -> use `Common.sumQty`
- Add import of `Carbonica.Validators.Common` (currently has NO Common imports)

**Marketplace.hs:**
- Remove `import Carbonica.Utils` entirely
- Remove local `payoutAtLeast` -> use `Common.payoutAtLeast`
- Remove local `isSignedBy` -> use `Common.isInList` (adapt: `isSignedBy pkh signatories` becomes `isInList pkh signatories`)
- Remove local `hasTokenPayment` -> use `Common.payoutTokenExact` (adapt if signature differs slightly)
- Add import of `Carbonica.Validators.Common` (currently has NO Common imports)

**ProjectPolicy.hs:**
- Remove local `getTokensForPolicy` -> use `Common.getTokensForPolicy`
- Remove local `isCategorySupported` -> use `Common.isCategorySupported`
- Remove local `allQtysNegative` -> use `Common.allNegative`
- Update import of `Carbonica.Validators.Common`

**IdentificationNft.hs:**
- Remove `import Carbonica.Utils` (used for tokenNameFromOref)
- Add `import Carbonica.Validators.Common (tokenNameFromOref)` (or add to existing Common import)

**CotPolicy.hs:**
- Remove `import Carbonica.Utils` if present (used for tokenNameFromOref or other helpers)
- Switch to `import Carbonica.Validators.Common` for those functions
- Update existing Common import to include any newly needed functions

After all validators are updated and compile:
- DELETE `src/Carbonica/Utils.hs`
- Remove `Carbonica.Utils` from `exposed-modules` in `smartcontracts.cabal`

CRITICAL: For every local function removed, ensure its Common.hs replacement is added to the import list in the SAME edit. Do not leave an ambiguous name in scope (Pitfall 2 from RESEARCH.md).

CRITICAL: After deleting Utils.hs, grep the entire `src/` directory for `import Carbonica.Utils` -- there must be zero matches (Pitfall 3 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd E:/smartcontracts && cabal build 2>&1 | tail -20 && echo "---CHECK UTILS IMPORTS---" && grep -r "import Carbonica.Utils" src/ || echo "No Utils imports remain (GOOD)"</automated>
  </verify>
  <done>All 7 validators import exclusively from Carbonica.Validators.Common. No local duplicate helpers remain. Utils.hs is deleted. Cabal exposed-modules updated. Project compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `cabal build` succeeds with no errors
2. `grep -r "import Carbonica.Utils" src/` returns no results
3. `grep -r "countMatchingSigners\|verifyMultisig" src/Carbonica/Validators/ | grep -v Common.hs` returns no results (no old duplicate names in validators)
4. File `src/Carbonica/Utils.hs` does not exist
5. `Carbonica.Utils` does not appear in `smartcontracts.cabal` exposed-modules
</verification>

<success_criteria>
- Common.hs has all shared helpers with INLINEABLE pragmas
- No validator has local helper duplicates
- Utils.hs is deleted
- All validators compile and import only from Common.hs for shared logic
- `cabal build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-code-quality-foundation/01-01-SUMMARY.md`
</output>
